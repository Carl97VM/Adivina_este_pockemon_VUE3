// import pockemonApi from '@/api/pockemonApi'

describe('pockemonApi', () => {

    test('Prueba de axios para la ruta de el API', () => {
        // para validar que la ruta sea siempre la misma
        // expect(pockemonApi.defaults.baseURL ).toBe('https://pokeapi.co/api/v2/pokemon')
        // console.log( pockemonApi )
    })

})